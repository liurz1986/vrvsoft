package cn.com.liurz.test.BuildBeetl;

import feign.template.Template;
/*import org.bee.tl.core.GroupTemplate;

import org.bee.tl.core.Template;*/

public class BeetlTest {
    public static void main(String[] args) {

     /*   String input = "Hello ${name}!";

        try{

            GroupTemplate group = new GroupTemplate();//使用空构造函数创建一个模板

            Template template = group.getStringTemplate(input);//模板是 String，通过 GroupTemplate 的 getStringTemplate 创建一个模板类 Template。"${""}"是模板里的占位符号。如果模板是一个文件，则需要 getFileTemplate，传入一个相对 root 的路径来创建一个模板类.

            template.set("name", "Beetl");//定义模板变量，设置变量值，注：变量名必须符合java命名规范

            String output = template.getTextAsString();//渲染输出模板引擎输出字符串

            System.out.println(output);

        }catch(Exception e){

            e.printStackTrace();

        }*/

    }
}
